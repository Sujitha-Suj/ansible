- name: package installation using the conditions
  hosts: frontend
  become: yes
  tasks:
    - name: package installation 
      package:
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      loop:
        - { name: "nginx", state: "present" }
        - { name: "mysql", state: "present" }
