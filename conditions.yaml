- name: demo on conditions
  hosts: local
  connection: local
  tasks:
    - name: print names
      debug:
        msg: "Hi {{ item }}"
      loop:
        - ramesh
        - raheem
        - sujitha