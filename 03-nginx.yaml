- name: install the nginx
  hosts: frontend
  become: yes
  tasks:
    - name: Install the nginx
      ansible.builtin.package:
        name: nginx
        state: present
        
    - name: start nginx
      service:
        name: nginx
        state: started
        enabled: yes
